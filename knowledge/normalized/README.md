# Normalized datasets

Every source/version contains `concepts.jsonl`, `occupations.jsonl`, `relations.jsonl`
and `manifest.json`. Normalization reads `knowledge/raw` and never writes into it.

Generated datasets are intentionally not committed to Git.
